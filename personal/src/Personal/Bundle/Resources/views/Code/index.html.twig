<html lang="en" class="no-js">
	<head>
        {% block stylesheets %}
            {% stylesheets 'bundles/personal/css/picks/default.css' filter='cssrewrite' %}
                <link rel="stylesheet" href="{{ asset_url }}" />
            {% endstylesheets %}
        {% endblock %}
        <title>{% block title %}Coding Bat Ripoff{% endblock %}</title>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
	</head>
	<body style="font-family:Helvetica">
        <div name="logout" style="display:inline-block;float:right;">
            <a href={{ path("compiler_logout") }}>
                <button class="login" style="width:80px;height:40px">Logout</button>
            </a>
        </div>
        <div name="header">
            <h2>CodingBat (Ripoff)</h2>
        </div>
        <div name="description">
            <p>
                The goal of this assignment is to simply test the capabailities of the CodingBat Ripoff Site!
            </p>
            <p>
                Add your code to the function below and when you think it's ready, click submit and see if your input works!<br>
                All libraries necessary to complete the task at hand will be included already.<br>
                Assume the standard namespace will be used for all assignments unless specificed otherwise.
            </p>
            <p>
                <strong>Prompt:</strong><br>
                Just to get started, make the method output "Hello World".
            </p>
        </div>
        <div name="code">
            <form name="codeform" action="" method="POST">
        		<textarea id="textbox" name="code" style="width:500px;resize:none;font-size:12px;border: 2px solid gray;" rows="10"autofocus>{% if code is defined %}{{ code }}{% else %}void basicTest()&#13;{&#13;&#09;&#13;}{% endif %}</textarea>
                <input type="hidden" value="void" name="type">
                <input type="hidden" value="basicTest()" name="name"><br>
                <input type="submit" value="Run!" class="login">
            </form>
        </div>
        {% if output is defined %}
            <div name="output">
                <p>
                    {% if pass and "Hello World" in output %}
                        <strong>Output:</strong>&nbsp;{{ output }}
                        <p style="color:#00cd00">
                            Success!
                        </p>
                    {% else %}
                        <p style="color:#ff0000">
                            Oops. Something's not quite right.
                        </p>
                        <p>
                            <i>Expected "Hello World" but was "{{ output }}"</i>
                        </p>
                    {% endif %}
                </p>
            </div>
        {% endif %}
	</body>
    {% block javascripts %}
        {% javascripts 'bundles/personal/js/code.js'%}
            <script src="{{ asset_url }}"></script>
        {% endjavascripts %}
    {% endblock %}
</html>
