<!DOCTYPE html>
<html lang="en" class="no-js">
	<head>
        {% block stylesheets %}
            {% stylesheets  'bundles/personal/css/foundation.min.css'
                            'bundles/personal/css/foundation-icons/foundation-icons.css'
                            'bundles/personal/css/index.css' filter='cssrewrite' %}
                <link rel="stylesheet" href="{{ asset_url }}" />
            {% endstylesheets %}
        {% endblock %}
        <title>{% block title %}MasteringC++{% endblock %}</title>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
        {% javascripts 'bundles/personal/js/vendor/modernizr.js' %}
            <script src="{{ asset_url }}"></script>
        {% endjavascripts %}
	</head>
    <body style="font-family:Helvetica">
        <div id="wrapper">
            <div id="header">
                <nav class="top-bar" data-topbar role="navigation" data-options="is_hover: false">
                    <ul class="title-area">
                        <li class="name">
                            <h1><a>MasteringC++</a></h1>
                        </li>
                        <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
                        <li class="toggle-topbar"><a href="#"><span>Menu</span></a></li>
                    </ul>

                    <section class="top-bar-section">
                        <!-- Right Nav Section -->
                        <ul class="right">
                    		{% if teacher %}
                                <li class="divider"></li>
                                <li>
                                    <a href={{ path("teacher_admin") }}>Home</a>
                                </li>
                                <li class="divider"></li>
                    		{% endif %}
                            <li class="has-dropdown">
                                <a href="#">Signed in as <strong>{{ email }}</strong> {% if teacher %}&nbsp;<i>Teacher</i>{% endif %}</a>
                                <ul class="dropdown">
                                    <li class="active">
                                        <a href="{{ path("compiler_logout") }}">Logout</a>
                                    </li>
                                </ul>
                            </li>
                        </ul>
                    </section>
                </nav>
            </div>
    		<div id="problems">
                <ul class="accordion" data-accordion>
    		    {% for key,value in problems %}
                {% if loop.first or key == "Custom" %}
                    <li class="accordion-navigation active">
                {% else %}
                     <li class="accordion-navigation">
                {% endif %}
                    <a href="#panel{{ loop.index }}a"><i class="fi-plus"></i><i class="fi-minus"></i>&nbsp;&nbsp;{{ key }}</a>
                        {% if loop.first or key == "Custom" %}
                            <div id="panel{{ loop.index }}a" class="content active">
                        {% else %}
                            <div id="panel{{ loop.index }}a" class="content">
                        {% endif %}
                    
        				{% set titles  = value['titles'] %}
        				{% set ids  = value['ids'] %}
        				{% for key,value in titles %}
        					<span class="problem">
        						{{ loop.index }}.&nbsp;<a class="problem" {% if value %}style="color:#00B137"{% endif %} href={{ path("compiler_problem", { 'problem' : ids[loop.index0] }) }}>{{ key }}</a>
        					</span>
        				{% endfor %}<br>
                      </div>
                    </li>
    		    {% endfor %}
                </ul>
    		</div>
            <div id="footer">
                <p>&copy; MasteringC++. All rights reserved.</p>
            </div>
        </div>
        {% block javascripts %}
            {% javascripts  'bundles/personal/js/vendor/jquery.js'
                            'bundles/personal/js/sticky-footer.js'
                            'bundles/personal/js/vendor/fastclick.js'
                            'bundles/personal/js/foundation.min.js' %}
                <script src="{{ asset_url }}"></script>
            {% endjavascripts %}
        {% endblock %}
        <script>
          $(document).foundation({
              accordion: {
                  multi_expand: true,
                  toggleable: true
              }
          });
        </script>
	</body>
</html>
